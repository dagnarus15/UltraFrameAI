<Window x:Class="UltraFrameAI.FfmpegInstallLocationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}"
        Width="560"
        SizeToContent="Height"
        MinWidth="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Background="Transparent">
    <Border Background="{DynamicResource AppBgBrush}"
            BorderBrush="{StaticResource CardBorderBrush}"
            BorderThickness="1"
            CornerRadius="18"
            ClipToBounds="True">
        <Grid Margin="18">
            <Border Style="{StaticResource CardStyle}" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="16" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="{Binding DialogTitle}"
                               FontSize="22"
                               FontWeight="SemiBold" />

                    <TextBlock Grid.Row="2"
                               Text="{Binding DialogMessage}"
                               TextWrapping="Wrap"
                               Foreground="{StaticResource TextSecondaryBrush}" />

                    <Grid Grid.Row="4"
                          HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                Content="{Binding ChooseFolderText}"
                                MinHeight="40"
                                Padding="16,8"
                                HorizontalAlignment="Stretch"
                                Click="ChooseFolder_Click" />
                        <Button Grid.Column="2"
                                Content="{Binding InstallHereText}"
                                MinHeight="40"
                                Padding="16,8"
                                HorizontalAlignment="Stretch"
                                Background="{StaticResource AccentBrush}"
                                Click="InstallHere_Click" />
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
